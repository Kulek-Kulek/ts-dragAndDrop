{
  "name": "typescript",
  "version": "1.0.0",
  "description": "draganddrop",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "okayProjects",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}